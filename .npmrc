# Allow critical packages to run post-install scripts (required for deployment)
# Prisma: needed for DB client; sharp: Next.js image optimization
# bcrypt excluded - app uses bcryptjs (pure JS), bcrypt has native build that can fail in minimal Docker
